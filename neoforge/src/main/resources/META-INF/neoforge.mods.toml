license="${mod_license}"

[[mods]]
modId="${mod_id}"
version="${version}"
displayName="${mod_name}"
description='''
Periodically sends a configurable chat message on an interval you control, so you don't get
kicked for being AFK. Configure via /afkspam or the mod's config screen (Mods > Config).
'''
authors="the-real-ltcg"

[[dependencies.${mod_id}]]
    modId="neoforge"
    type="required"
    versionRange="[26.2,)"
    ordering="NONE"
    side="CLIENT"

[[dependencies.${mod_id}]]
    modId="minecraft"
    type="required"
    versionRange="${minecraft_version_range}"
    ordering="NONE"
    side="CLIENT"
